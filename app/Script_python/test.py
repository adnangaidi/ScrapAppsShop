# import sys

# # Retrieve command-line arguments
# if len(sys.argv) > 1:
#     url = sys.argv[0]
#     # You can access additional arguments if needed

#     # Now you can use 'url' in your script as the passed data
#     print(url)
# else:
#     print("No URL provided as an argument.")

tipe=[
    'gama',
    'alpha',
    'beta'
]
for  i in tipe:
    if i=='gama':
         print('yang kamu pilih adalah {}'.format(i))
    else:
         print('yang kam{}'.format(i))
         